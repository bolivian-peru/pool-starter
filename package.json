{
  "name": "pool-starter",
  "private": true,
  "description": "Open-source reseller toolkit for the Proxies.sx Pool Gateway — SDK, drop-in React component, and Next.js starter template.",
  "repository": {
    "type": "git",
    "url": "https://github.com/bolivian-peru/pool-starter"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build",
    "test": "pnpm -r --filter './packages/*' test",
    "typecheck": "pnpm -r --filter './packages/*' typecheck",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=18.17.0"
  },
  "license": "MIT"
}
